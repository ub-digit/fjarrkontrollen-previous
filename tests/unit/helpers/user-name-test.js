import {
  userName
} from 'fjarrkontroll-cli/helpers/user-name';

module('UserNameHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = userName(42);
  ok(result);
});
