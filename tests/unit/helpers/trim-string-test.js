import {
  trimString
} from 'fjarrkontroll-cli/helpers/trim-string';

module('TrimStringHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = trimString(42);
  ok(result);
});
