import {
  getSortDirArrow
} from 'fjarrkontroll-cli/helpers/get-sort-dir-arrow';

module('GetSortDirArrowHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = getSortDirArrow(42);
  ok(result);
});
