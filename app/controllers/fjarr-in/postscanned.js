import Ember from 'ember';
import ENV from '../../config/environment';
import PostController from 'fjarrkontroll-cli/controllers/fjarr-in/post';

export default PostController.extend(Ember.Evented, {

});
