<div class="container">
  {{#if error}}
  <div class="alert alert-danger col-md-offset-3 col-md-6 col-lg-offset-4 col-lg-4">
    {{error}}
  </div>
  {{/if}}
  <div class="panel panel-default col-md-offset-3 col-md-6 col-lg-offset-4 col-lg-4">
    <div class="panel-body">
  <form {{action 'authenticate' on='submit'}} role="form">
    <div class="form-group">
      <label for="identification">X-konto</label>
      {{input value=identification classBinding=":form-control" placeholder='X-konto'}}
    </div>
    <div class="form-group">
      <label for="password">Lösenord</label>
      {{input value=password classBinding=":form-control" placeholder='Lösenord' type='password'}}
    </div>
    <button class="btn btn-primary" type="submit">Login</button>
  </form>
  </div>
  </div>
</div>
